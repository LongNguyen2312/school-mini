/**
 * KTV playlist — drop free/licensed tracks in public/assets/audio/
 * and add the filename here.
 * Playback is server-synced (shared timeline); first track loops for now.
 */
export const KTV_TRACKS = ['vinahouse3.mp3'] as const

/** Target playback volume (0–1). */
export const KTV_VOLUME = 0.4
